% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gen_sim_cat.R
\name{gen_sim_cat}
\alias{gen_sim_cat}
\title{Simulate categorical mixed-effects data for GMERT}
\usage{
gen_sim_cat(
  n = 1000,
  n_groups = 20,
  n_vars = 10,
  n_random = 3,
  K = 3,
  rho = 0.3,
  beta = NULL,
  mu = 0,
  random_slope_sd = 0.5,
  intercept_sd = 0.8,
  intercept_slope_cor = 0.1,
  slope_corr = 0.1,
  D = NULL,
  seed = NULL,
  random_vars = NULL,
  intercept = 0,
  return_prob = FALSE
)
}
\arguments{
\item{n}{Integer. Total number of observations to generate.}

\item{n_groups}{Integer. Number of groups for group-level random effects.}

\item{n_vars}{Integer. Total number of predictors to simulate.}

\item{n_random}{Integer. Number of predictors that receive group-level random slopes.}

\item{K}{Integer. Number of response classes. Must be at least 3.}

\item{rho}{Numeric in [0, 1). Pairwise correlation used to build a
compound-symmetry covariance matrix for predictors.}

\item{beta}{Numeric matrix or NULL. Fixed-effect coefficient matrix of dimension
\code{n_vars x (K-1)}. When NULL, coefficients are sampled from N(0, 1).}

\item{mu}{Numeric scalar or numeric vector of length \code{n_vars}. Mean(s) used
when drawing predictors.}

\item{random_slope_sd}{Numeric scalar or numeric vector of length \code{n_random}.
Standard deviation(s) for each random slope within each contrast.}

\item{intercept_sd}{Numeric scalar. Standard deviation for the group intercepts
within each contrast.}

\item{intercept_slope_cor}{Numeric scalar or numeric vector of length
\code{n_random}. Correlation(s) between the intercept and each slope within
each contrast.}

\item{slope_corr}{Numeric scalar or numeric matrix. If scalar, used as a common
pairwise correlation among slopes within each contrast; if matrix, must be
\code{n_random x n_random}.}

\item{D}{Optional full random-effects covariance matrix of dimension
\code{((K-1)*(1+n_random)) x ((K-1)*(1+n_random))}. If supplied, it overrides
the default block-diagonal construction.}

\item{seed}{Integer or NULL. Optional RNG seed for reproducibility.}

\item{random_vars}{Integer vector or NULL. Indices of the predictors that receive
random slopes; if NULL random indices are selected.}

\item{intercept}{Numeric scalar or numeric vector of length \code{K-1}. Fixed
intercept(s) for the non-reference classes.}

\item{return_prob}{Logical. If TRUE, append the true class probabilities to the
output data frame.}
}
\value{
A data.frame with columns \code{id}, \code{y}, and predictors
\code{x1}, ..., \code{xP}. If \code{return_prob = TRUE}, the class
probabilities \code{prob_1}, ..., \code{prob_K} are also returned.
}
\description{
Generate a clustered dataset for a multinomial logit mixed-effects model with
group-level random intercepts and random slopes. The response has \code{K}
classes, with class \code{K} used as the reference category.
}
\details{
For each observation \eqn{i} and non-reference class \eqn{k = 1, \dots, K-1},
the linear predictor is
\deqn{
\eta_{ik} = \alpha_k + x_i^\top \beta_k + z_i^\top b_{g(i)k},
}
where \eqn{g(i)} is the group of observation \eqn{i}. Class probabilities are
obtained through the multinomial softmax with class \eqn{K} as the reference:
\deqn{
P(Y_i = k) = \frac{\exp(\eta_{ik})}{1 + \sum_{\ell=1}^{K-1}\exp(\eta_{i\ell})},
\qquad
P(Y_i = K) = \frac{1}{1 + \sum_{\ell=1}^{K-1}\exp(\eta_{i\ell})}.
}

By default, the random-effects covariance is block-diagonal across contrasts,
with the same within-contrast covariance repeated for each of the \code{K-1}
logits. To simulate cross-contrast dependence, supply a full covariance matrix
through \code{D}.
}
