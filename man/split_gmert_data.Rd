% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/split_gmert_data.R
\name{split_gmert_data}
\alias{split_gmert_data}
\title{Train/test split by cluster for GMERT data}
\usage{
split_gmert_data(df, train_prop = 0.7, seed = 123)
}
\arguments{
\item{df}{A data.frame containing clustered observations. Must include
a column named \code{id}.}

\item{train_prop}{Numeric. Proportion of observations per cluster to use
for training.}

\item{seed}{Integer. Random seed for reproducibility.}
}
\value{
A list with two elements:
\item{train}{Training data.frame}
\item{test}{Test data.frame}
}
\description{
Splits a clustered dataset into training and test sets while preserving
the cluster structure. Each cluster contributes the same proportion of
observations to the training and test sets.
}
\examples{
data <- sim_data_gmert()
split <- split_gmert_data(data)
nrow(split$train)
nrow(split$test)

}
\seealso{
Other gmert: 
\code{\link{fit_gmerf_small}()},
\code{\link{fit_gmert}()},
\code{\link{fit_gmert_small}()},
\code{\link{predict_gmert}()},
\code{\link{sim_data_gmert}()}
}
\concept{gmert}
